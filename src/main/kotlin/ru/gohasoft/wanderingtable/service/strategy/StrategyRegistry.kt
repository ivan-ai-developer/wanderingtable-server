package ru.gohasoft.wanderingtable.service.strategy

/**
 * Реестры стратегий.
 *
 * Spring внедряет все реализации интерфейса, реестр раскладывает их по типу. Добавление
 * второй реализации (например, сетки на выбывание) — это новый класс с новым значением
 * enum: ни сервисы, ни контроллеры не меняются.
 */
interface StrategyRegistry

