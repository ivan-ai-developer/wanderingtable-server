package ru.gohasoft.wanderingtable.service.strategy

/**
 * Типы стратегий. Хранятся в турнирных событиях как строки и служат ключом реестра,
 * поэтому добавление новой реализации сводится к новой константе и новому бину —
 * вызывающий код не меняется.
 */
interface StrategyType